# 4모델 F1 비교

## 결과

| 순위 | 모델 | F1 (라벨 1 기준) |
|:---:|---|:---:|
| 1 | 랜덤 포레스트 | **0.5768** |
| 2 | 그래디언트 부스팅 | 0.4885 |
| 3 | SVM (RBF) | 0.3798 |
| 4 | 로지스틱 회귀 | 0.3195 |

## 실험 조건

| 항목 | 내용 |
|---|---|
| 데이터 | 좌표 결측 제거 후 3,213행 |
| 라벨 | `청소년포함` (모집대상에 '청소년' 포함 시 1, 아니면 0) |
| 분할 | `train_test_split(test_size=0.2, random_state=42, stratify=라벨)` |
| 학습 / 테스트 | 2,570행 / 643행 (테스트 라벨 1: 245행, 라벨 0: 398행) |
| 평가 지표 | 테스트셋 F1, 라벨 1을 양성 클래스로 계산 |

## 입력 변수

- 범주형(원-핫 인코딩): `모집분야`, `활동유형`
- 수치형: `모집인원`, `신청인원`, `봉사기간일수`, `모집기간일수`, `시작시각`, `위도`, `경도`
- 로지스틱 회귀와 SVM은 수치형 변수를 표준화했고, 트리 계열 모델은 원값을 그대로 사용했습니다.

## 제외한 변수

- `모집대상`: 라벨의 원천이라 그대로 넣으면 정답이 새어 들어갑니다.
- `청소년포함비율`: 전체 데이터의 라벨로 계산된 값이라 테스트셋 정보가 섞입니다.
- `비대면비율`: 모든 행이 0인 상수입니다.
- `등록기관수`, `프로그램수`: 모집분야별 값이라 `모집분야`와 정보가 중복됩니다.

## 참고

- 하이퍼파라미터는 튜닝 없이 기본값을 사용했고(랜덤 포레스트는 `n_estimators=300`), 모든 모델에 `random_state=42`를 적용했습니다.
- 단일 분할 결과이므로 모델 간 작은 차이는 분할에 따라 달라질 수 있습니다. 교차검증을 하면 더 안정적인 비교가 됩니다.
